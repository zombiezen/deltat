env DELTAT_DB=$PWD/foo.sqlite

deltat start -d Smoke testing
sleep 10m15s
deltat stop

deltat timesheet --all --format=plain
stdout '^- .*: Smoke testing$'
stdout '^\| Smoke testing\s*\| 0:10:15 \|$' # Make sure the time in the totals isn't negative.
